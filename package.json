{
  "name": "test-module",
  "version": "1.1.0",
  "description": "test for private npm repository",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
